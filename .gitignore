# Chromium's compiled static-ruleset cache. Regenerated by the browser on every
# load of the unpacked extension -- never commit it.
_metadata/

# Packaged archives
*.zip

# Node (the build script has no dependencies, but keep this for safety)
node_modules/
npm-debug.log*

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
